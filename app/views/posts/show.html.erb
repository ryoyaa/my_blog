<div class="col-md-12">
  <h1><%= @post.title %></h1>
  <p>カテゴリ：<%= @post.category %></p>
  <p>登校日：<%= @post.created_at %></p>
  <hr>
    <%= simple_format(@post.body) %>
  <hr>
  <%= link_to '編集', edit_post_path(@post), class: "btn btn-default" %>
  <%= link_to '削除', post_path(@post), class: "btn btn-default", method: :delete %>
</div>
